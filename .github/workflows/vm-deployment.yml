name: Deploy to Azure

on:
  push:
    branches:
      - beta

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Install SSH Client
      run: sudo apt-get install -y openssh-client

    - name: Deploy to Azure
      run: |
        sshpass -p "${{ secrets.SSH_PASSWORD }}" ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null rad449@104.42.49.47 "cd C:\Users\rad449\Desktop\Github && git pull origin beta"
      env:
        SSH_PASSWORD: ${{ secrets.SSH_PASSWORD }}
