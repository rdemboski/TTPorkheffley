name: Deploy to Mac Mini

on:
  push:
    branches:
      - beta

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Install SSH Client
      run: sudo apt-get install -y openssh-client

    - name: Deploy to Mac Mini
      run: |
        sshpass -p 'SSOnic12$$' ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null ryandemboski@47.215.135.41 "cd /Users/ryandemboski/Desktop/GithubAction && git pull origin beta"
